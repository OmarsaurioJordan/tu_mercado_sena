para crear un EXE:

primero tener instalado y actualizado pyinstaller:
pip install pyinstaller
pip install --upgrade pyinstaller

luego correr este comando en la carpeta raíz del proyecto:
pyinstaller --onefile --windowed --icon=icono.ico --collect-all PySide6 --add-data "assets;assets" main.py
