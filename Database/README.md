# Database

se incluye la DB en formato SQL, **tu_mercado_sena**

incluye una imágen del modelo **MR** sacado de la DB

hay un software en Python (**PoblarDB**) para poblar la DB con miles de datos sintéticos aleatorios

la carpeta **TMS_API** es una API sin framework, sencilla, para manejar la DB, utilizada por el entorno Desktop
